Техническое задание

а) Написать на C++ программу, которая реализует функцию int GetNext(),
   функция должна возвращать элементы арифметической прогрессии
   2*n*n+3*n+5, функция должна корректно работать в многопоточной среде.

б) Создать 2 потока, которые будут вызывать функцию GetNext и записывать
   результат в один и тот же текстовый файл
   (вывести в файл результат для n от 0 до 10000).

